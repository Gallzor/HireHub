<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.HireHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Hier voeg je de stijl toe voor je achtergrondafbeelding. -->
        <item name="android:windowBackground">@drawable/background_image_hirehub_blur</item>
        <item name="android:textColor">@color/black</item> <!-- Tekstkleur voor Day-modus -->
    </style>

    <style name="Theme.Hirehub" parent="Base.Theme.HireHub" />
    <!-- Base application theme. -->

    <style name="Base.Theme.Hirehub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Hier voeg je de stijl toe voor je achtergrondafbeelding. -->
        <item name="android:windowBackground">@drawable/background_image_hirehub_blur</item>
        <item name="android:textColor">@color/white</item> <!-- Tekstkleur voor Night-modus -->
    </style>
</resources>